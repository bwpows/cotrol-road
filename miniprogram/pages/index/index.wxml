<!-- <view class="map_container">
  <map longitude="{{longitude}}" latitude="{{latitude}}" scale="14" class="map" id="map" show-location="{{true}}" enable-rotate="true" markers="{{markers}}" bindtap="maptap" bindmarkertap="makertap" />
</view>
<view class="map_text">
  <text class="h1">{{textData.name}}</text>
  <text>{{textData.desc}}</text>
</view> -->

<view class="map_container">
  <map class="map" id="map" longitude="{{longitude}}" latitude="{{latitude}}" scale="14" show-location="true" markers="{{markers}}" bindmarkertap="makertap" polyline="{{polyline}}"></map>
</view>
<view class="map_text">
  <text class="h1">{{textData.name}}</text>
  <text>{{textData.desc}}</text>
</view>
